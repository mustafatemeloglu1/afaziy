{
  "name": "afazi-terapi",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "web": "python3 -m http.server 8080 --directory www",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  },
  "description": "Genişletilmiş, internetten güncellenebilir Afazi Terapi DKT uygulaması"
}
