import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.afaziterapi.app',
  appName: 'Afazi Terapi',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
